// this is the constant for the auth module. It is used to store the constants for the auth module. Constants are used to store the values that are not going to change. In this case, the ROLES_KEY is used to store the roles for the authenticated user. We will use the ROLES_KEY in the @Roles decorator to specify the roles for the authenticated user.

export const ROLES_KEY = 'roles';
